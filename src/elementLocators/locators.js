/**
 * This file contains locators for all the elements of ios and Android app
 */

const iOSLocators = {
    // iOS locators to be add here
};

const AndroidLocators = {
    // Forms page
    Forms: '~Forms',

    // Home page
    Home: '~Home',
    'Home screen': '~Home-screen',

    // Login page
    Login: '~Login',

    // Swipe page
    Swipe: '~Swipe',

    // WebView page
    Webview: '~WebView',
};

const CommonLocators = {
    // Common locators of iOS & Android to be add here
};

export { iOSLocators, AndroidLocators, CommonLocators };
